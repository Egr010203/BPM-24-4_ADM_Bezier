# План работ по проекту

## День 1 — анализ задания и подготовка проекта

Готово:

- изучены требования заданий 3.1 и 3.2;
- выбран язык программирования — Python;
- выбраны библиотеки — NumPy и Matplotlib;
- подготовлена структура проекта;
- выбран контур для задания 3.2 — стилизованное пламя;
- подготовлено техническое задание для команды;
- описаны контрольные точки для элементарной и составной кривой;
- определены ожидаемые изображения для отчёта и презентации.

## День 2–3 — элементарная кривая Безье

Ответственный: Человек 2.

Готово:

- реализована функция `cubic_bezier()` (формула Бернштейна, возвращает массив shape (n, 2));
- реализована функция `bezier_coefficients()` — коэффициентная форма ax, bx, cx, dx / ay, by, cy, dy;
- реализована функция `plot_elementary_curve()` — сохраняет PNG с кривой, контрольным многоугольником и подписями точек;
- `images/elementary_curve.png` сгенерирован;
- `src/main.py` дополнен функцией `run_task_31()` с выводом коэффициентов в консоль.

## День 4 — составная кривая Безье

Ответственный: Человек 3.

Нужно сделать:

- реализовать функцию `composite_bezier()`;
- использовать сегменты контура пламени;
- проверить замкнутость и отсутствие разрывов;
- подготовить таблицу точек для отчёта.

## День 5 — масштабирование и визуализация

Ответственный: Человек 4.

Нужно сделать:

- реализовать `scale_points()`;
- построить контур при масштабах `0.5`, `1`, `1.5`, `2`;
- сохранить изображения в папку `images/`.

## День 6 — отчёт

Ответственный: Человек 5.

Нужно сделать:

- собрать теорию, формулы, код, графики и выводы;
- оформить список литературы;
- привести документ к единому стилю.

## День 7 — презентация и защита

Ответственный: Человек 6.

Нужно сделать:

- подготовить 10–12 слайдов;
- добавить графики;
- подготовить краткую речь для защиты;
- проверить, что все участники понимают свои части.
